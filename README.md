# payments-api

Internal payments API. Deployed to `ikt-workshop` from `deploy/`.

## Automated review

Kubernetes manifest changes are reviewed by the platform team's PR review
agent. The agent loads review procedures from `.agent/skills/` in this
repository, so the procedure lives next to the code it reviews.
